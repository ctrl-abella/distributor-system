export type CheckoutForm = {
    fullName: string;
    contactNumber: string;
    email: string;

    region: string;
    province: string;
    city: string;
    address: string;
    postalCode: string;

    paymentMethod: string;
};